import { Component } from '@angular/core';

@Component({
    selector: 'liste-amis-connect',
    templateUrl : './liste-amis-connect.component.html',
    styles: [`
        .carteami {
            margin: 4px;
        }
    `]
})

export class ListeAmisConnect {
    
}